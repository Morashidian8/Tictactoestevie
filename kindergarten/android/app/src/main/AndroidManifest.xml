<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      اینترنت لازم است، ولی نبودنش اپ را نمی‌کشد: سرویس‌ورکرِ خودِ برنامه
      پوستهٔ اپ را پیش‌ذخیره می‌کند و بعد از اولین اجرا آفلاین بالا می‌آید.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      دوربین برای عکس گرفتن از مدرک و کودک.
      `required=false` تا گوشیِ بی‌دوربین هم بتواند نصبش کند؛ انتخاب از
      گالری همیشه هست.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
      دیدنِ اپِ دوربین.

      از اندروید ۱۱، `resolveActivity` برای اپی که اینجا اعلام نشده باشد
      همیشه تهی برمی‌گرداند — یعنی کلید «دوربین» بی‌صدا ناپدید می‌شد.
    -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.Mahd">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          نشانیِ امنِ عکسِ دوربین.

          اپِ دوربین در فایلی می‌نویسد که ما می‌سازیم و نشانی‌اش را
          موقتاً به او می‌دهیم — نه دسترسی به حافظه.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
