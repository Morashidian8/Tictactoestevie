# تاریخ، ارقام و قالب‌های متن

بخش ۱۲.۴ سند.

خط قرمز: هیچ تاریخ میلادی در رابط کاربری دیده نمی‌شود. تنها تابعی که خروجی
میلادی می‌دهد `toIsoDate` است و فقط برای ستون `date` در لایه داده است.

تقویم جلالی از `Intl` می‌آید، بدون کتابخانه، و ارقام فارسی را خودش می‌دهد.
