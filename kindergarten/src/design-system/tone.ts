/**
 * لحن هر نقش — رنگِ آسمان، شکلِ شخصیت، و صحنهٔ سرصفحه.
 *
 * در فایل خودش نشسته، نه در `AppShell.tsx`، چون `SkyScene` هم لازمش
 * دارد و وارد کردنش از پوسته یک حلقهٔ وابستگی می‌ساخت.
 *
 * تنها چیزی که بین سه پنل عوض می‌شود همین است. ساختار یکی می‌ماند، چون
 * مربی و مدیر گاهی یک نفرند و نباید دوبار یاد بگیرند کجا را بزنند.
 */
export type ShellTone = 'teacher' | 'parent' | 'manager'
