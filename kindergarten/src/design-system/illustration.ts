/**
 * پالت تصویرسازی — جدا از توکن‌های رابط، و عمداً.
 *
 * بخش ۱۲.۲ پالت رابط را می‌بندد: یازده رنگ، و هیچ جزئی حق ندارد مقدار
 * خام بنویسد. این‌ها آن رنگ‌ها نیستند. رنگ پوست و مو تصویرسازی‌اند، نه
 * رنگ رابط؛ نه معنا حمل می‌کنند، نه وضعیت، و نه در هیچ کنشی به کار
 * می‌روند. اگر وارد پالت رابط می‌شدند، «رنگ‌های نظام طراحی» نوزده تا
 * می‌شد و هیچ‌کدام دیگر نقش روشنی نداشت.
 *
 * تنها قاعده‌شان این است: در یک فایل بمانند، نه پخش در کامپوننت.
 *
 * رنگ لباس از پالت رابط وام گرفته شده تا چهره‌ها با اپ یکی به نظر
 * برسند؛ به همین دلیل هم مقدارش اینجا تکرار نشده و از var خوانده
 * نمی‌شود — SVG درون کامپوننت به مقدار ثابت نیاز دارد.
 */

/** رنگ پوست. ترتیب معنا ندارد؛ انتخاب از روی شناسه کودک است. */
export const SKIN = ['#F4D2AE', '#E7B78C', '#D19A6C', '#B0764B', '#8D5A38'] as const

/** رنگ مو. */
export const HAIR = ['#2B2724', '#43301F', '#6B4A2E', '#141312', '#8A5A33'] as const

/**
 * رنگ لباس. همان مقادیر --coral، --mint، --mango، --brick و
 * --ink-muted از بخش ۱۲.۲‌اند. اگر پالت رابط عوض شود، این‌ها هم باید
 * دستی عوض شوند؛ تست پالت همین را یادآوری می‌کند.
 */
export const WEAR = ['#FF6B6B', '#10B981', '#F59E0B', '#EF4444', '#64748B'] as const

/** خط چشم و دهان. تیره‌تر از مو تا روی هر پوستی دیده شود. */
export const FEATURE_LINE = '#332924'
