version: "3.9"

services:
  polybot:
    build: .
    environment:
      POLYBOT_TOKEN: ${POLYBOT_TOKEN:-dev-token}
    ports:
      - "8700:8700"
    volumes:
      - ./polybot:/app/polybot
    command: uvicorn polybot.api:app --host 0.0.0.0 --port 8700 --reload
