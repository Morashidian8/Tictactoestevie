.env
__pycache__/
*.pyc
*.log
.cache/
.state/
*.avm
*.prover
*.verifier
outputs/
__pycache__/
*.pyc
.pytest_cache/
.chat_id
bot.log
breakout_closes.json
.bot.pid
.bot.loop.pid
.bot.stop

# Intermediate research pickles: regenerable by the task_*.py scripts and
# rewritten on every run, so they are handoff data between agents, not history.
research/btc5m/out/

# Recorded market data (Phase 2 onward): high-volume, regenerable only by
# re-recording, and far too large for git.
polymarket_bot/data/*
!polymarket_bot/data/.gitkeep
