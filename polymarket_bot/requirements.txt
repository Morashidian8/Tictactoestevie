# Phase 1 (collectors) — everything below is actually imported today.
websockets>=12.0,<16
httpx>=0.27,<1
PyYAML>=6.0,<7

# Tests
pytest>=8.0
pytest-asyncio>=0.23

# Later phases, listed but not yet imported. Install when the phase lands
# rather than now — an unused dependency is an unreviewed dependency.
#   Phase 2 recorder:   pyarrow>=16      (Parquet)
#   Phase 3 features:   numpy>=1.26
#   Phase 4 model:      scikit-learn>=1.4   (then lightgbm/xgboost, if ever)
#   Phase 6 backtest:   pandas>=2.2
#   Phase 9 execution:  py-clob-client   (Polymarket's own signed-order client)
#   Phase 19 storage:   psycopg[binary]>=3.1, redis>=5.0
